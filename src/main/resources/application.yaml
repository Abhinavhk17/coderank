spring:
  application:
    name: coderank

  # MongoDB Configuration
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:coderank}
      # For authenticated MongoDB, use:
      # uri: mongodb://${MONGO_USER:admin}:${MONGO_PASSWORD:password}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:coderank}?authSource=admin

  # H2 Database -  (using MongoDB instead)
  #datasource:
  #  url: jdbc:h2:mem:coderank
  #  username: sa
  #  password:
  #  driver-class-name: org.h2.Driver

  #h2:
  #  console:
  #    enabled: true
  #    path: /h2-console

  #jpa:
  #  hibernate:
  #    ddl-auto: update
  #  show-sql: true
  #  properties:
  #    hibernate:
  #      dialect: org.hibernate.dialect.H2Dialect
  #      format_sql: true


# JWT Configuration
jwt:
  secret: ${JWT_SECRET:coderank-secret-key-change-this-in-production-2026}
  expiration: 86400000 # 24 hours in milliseconds

# Code Execution Configuration
execution:
  timeout: 10 # seconds

# Server Configuration
server:
  port: 8082
  error:
    include-message: always
    include-binding-errors: always

# Logging
logging:
  level:
    com.coderank.api: INFO
    org.springframework.security: DEBUG

